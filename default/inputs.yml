inputs:
  in_aws_sagemaker_invocation:
    disabled: false
    sendToRoutes: true
    pqEnabled: false
    streamtags: []
    fileFilter: /.*/
    awsAuthenticationMethod: auto
    signatureVersion: v4
    reuseConnections: true
    rejectUnauthorized: true
    staleChannelFlushMs: 10000
    maxMessages: 1
    visibilityTimeout: 600
    numReceivers: 1
    socketTimeout: 300
    skipOnError: false
    includeSqsMetadata: false
    enableAssumeRole: true
    durationSeconds: 3600
    enableSQSAssumeRole: true
    preprocess:
      disabled: true
    parquetChunkSizeMB: 5
    parquetChunkDownloadTimeout: 600
    checkpointing:
      enabled: false
    pollTimeout: 10
    tagAfterProcessing: false
    encoding: utf8
    type: s3
    queueName: '"nth-sagemaker-invocation-notifications"'
    assumeRoleArn: arn:aws:iam::821601040136:role/CriblSageMakerLogsReader
    region: us-east-1
    metadata:
      - name: __packsource
        value: "'cribl-aws-sagemaker-io.invocation'"
    __template_region: aws_region
    __template_assumeRoleArn: iam_role_arn
    __template_queueName: sagemaker_invocation_sqs_queue
    breakerRulesets:
      - Cribl
  in_aws_sagemaker_cloudtrail:
    disabled: false
    sendToRoutes: true
    pqEnabled: false
    streamtags: []
    fileFilter: /.*/
    awsAuthenticationMethod: auto
    signatureVersion: v4
    reuseConnections: true
    rejectUnauthorized: true
    staleChannelFlushMs: 10000
    maxMessages: 1
    visibilityTimeout: 600
    numReceivers: 1
    socketTimeout: 300
    skipOnError: false
    includeSqsMetadata: false
    enableAssumeRole: true
    durationSeconds: 3600
    enableSQSAssumeRole: true
    preprocess:
      disabled: true
    parquetChunkSizeMB: 5
    parquetChunkDownloadTimeout: 600
    checkpointing:
      enabled: false
    pollTimeout: 10
    tagAfterProcessing: false
    encoding: utf8
    type: s3
    queueName: '"nth-sagemaker-cloudtrail-notifications"'
    assumeRoleArn: arn:aws:iam::821601040136:role/CriblSageMakerLogsReader
    region: us-east-1
    metadata:
      - name: __packsource
        value: "'cribl-aws-sagemaker-io.cloudtrail'"
    __template_region: aws_region
    __template_assumeRoleArn: iam_role_arn
    __template_queueName: sagemaker_cloudtrail_sqs_queue
    breakerRulesets:
      - AWS Ruleset
